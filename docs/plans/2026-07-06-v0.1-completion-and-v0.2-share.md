# V0.1 收口 + V0.2 分享传播版 实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 补全 V0.1 后台配置缺口（系统设置/内容配置/操作日志/归档/角色化 UI），并开发 V0.2 分享传播版（微信页面分享、决策卡 Canvas 分享图、小程序码、分享数据统计）。

**Architecture:** 三个里程碑递进。M1 纯后台改动（不影响线上小程序，可独立发版），M2 小程序赛前清单接接口，M3 V0.2 分享功能（小程序发版审核）。后端仍是单文件 Express，新增 wxacode 模块拆分独立文件。后台从单文件 App.tsx 抽取页面到 `src/pages/`。分享图用小程序端 Canvas 2D 生成，小程序码由后端调微信 API。

**Tech Stack:** Node.js + TypeScript + Express + Prisma（后端）；React 18 + Ant Design 5 + Vite + React Router 6（后台）；微信原生小程序 TypeScript（小程序端）。

---

## 现状关键事实（实现时参考）

- 后台 `apps/admin/src/App.tsx` 单文件 1076 行，含所有页面组件 + 路由 + 布局。
- 后端 `apps/api/src/server.ts` 单文件 994 行，所有 system-configs / operation-logs / events API 已就绪。
- 后端枚举在 `server.ts:66-83`（`as const` 数组）、`packages/shared/src/index.ts`、`packages/database/prisma/schema.prisma` 三处重复。
- 后台 `Shell` 组件（App.tsx:119-174）侧边栏仅 3 项；`ProtectedShell`（App.tsx:96-117）已获取 `admin` 对象含 role。
- 后端 `hashPassword`（server.ts:364-368）、`writeOperationLog`（server.ts:409-429）可直接复用。
- 小程序 `utils/api.ts` 的 `request` 封装支持 `silent`/`loadingText`；`getUserKey`（utils/user.ts）提供匿名标识。
- 小程序赛前清单 `pages/checklist/index.ts` 硬编码 5 组数据，未调用 `getChecklistTemplates()`（utils/api.ts:169）。
- 后端 `/api/checklist/templates`（server.ts:954-971）返回固定 7 项，不读 system_config。

---

## 里程碑 M1：V0.1 后台收口（纯后台，不影响小程序）

### Task M1-1: 抽取后台页面到独立文件（前置重构）

**Files:**
- Create: `apps/admin/src/pages/LoginPage.tsx`
- Create: `apps/admin/src/pages/WorkbenchPage.tsx`
- Create: `apps/admin/src/pages/EventsPage.tsx`
- Create: `apps/admin/src/pages/EventEditPage.tsx`
- Create: `apps/admin/src/pages/QualityPage.tsx`
- Create: `apps/admin/src/components/OperationLogTable.tsx`
- Create: `apps/admin/src/components/Section.tsx`
- Create: `apps/admin/src/utils/form.ts`（`defaultChecklist`、`splitComma`、`splitLines`、`toTextList`、`buildMiniappPublishChecks`）
- Create: `apps/admin/src/constants.ts`（`publishStatusOptions`、`infoStatusOptions`、`signupStatusOptions`、`runJudgementOptions`、`sourceLevelOptions`、`feedbackStatusOptions`、`riskKeywords`）
- Modify: `apps/admin/src/App.tsx`（仅保留 `App`、`ProtectedShell`、`Shell`，引用上述模块）

**Step 1: 抽取常量文件**

将 App.tsx:55-83 的所有 options 数组和 `riskKeywords` 移到 `apps/admin/src/constants.ts`，从各页面 import。

**Step 2: 抽取工具函数**

将 App.tsx:1027-1066 的 `defaultChecklist`、`splitComma`、`splitLines`、`toTextList` 移到 `apps/admin/src/utils/form.ts`。将 `buildMiniappPublishChecks`（App.tsx:821-863）和 `MiniappPublishChecks` 组件一起移到 `utils/form.ts` 或保留为 EventEditPage 的内部组件。

**Step 3: 抽取共享组件**

`Section`（App.tsx:1018-1025）→ `apps/admin/src/components/Section.tsx`。
`OperationLogTable`（App.tsx:970-999）→ `apps/admin/src/components/OperationLogTable.tsx`。
`EventLogsModal`（App.tsx:993-1016）→ 移入 EventsPage 或单独组件。

**Step 4: 逐个抽取页面**

每个页面文件 export 对应组件函数，import 所需的 antd 组件、api 函数、constants、utils。`showError`、`logout` 等辅助函数移到 `apps/admin/src/utils/helpers.ts`。

**Step 5: 精简 App.tsx**

App.tsx 仅保留：`App`（路由根）、`ProtectedShell`、`Shell`（布局+侧边栏+子路由）。import 各页面组件。

**Step 6: 验证**

Run: `pnpm typecheck && pnpm build`
Expected: PASS，后台功能不变。

**Step 7: Commit**

```bash
git add apps/admin/src/
git commit -m "refactor(admin): 抽取页面与组件到独立文件，精简 App.tsx"
```

---

### Task M1-2: 新增后端管理员管理接口

**Files:**
- Modify: `apps/api/src/server.ts`（在 system-configs 路由前后插入 admin-users 路由，约 L815 后）

**Step 1: 定义 Zod schema**

在 server.ts schema 区（约 L160 后）新增：

```typescript
const adminUserCreateSchema = z.object({
  username: z.string().trim().min(1, '用户名不能为空').max(50),
  password: z.string().min(6, '密码至少 6 位'),
  displayName: z.string().trim().min(1, '显示名不能为空'),
  role: z.enum(['super_admin', 'event_operator', 'content_reviewer', 'readonly']),
});

const adminUserUpdateSchema = z.object({
  role: z.enum(['super_admin', 'event_operator', 'content_reviewer', 'readonly']).optional(),
  status: z.enum(['active', 'disabled']).optional(),
  password: z.string().min(6).optional(),
  displayName: z.string().trim().min(1).optional(),
});
```

**Step 2: 实现 GET 列表接口**

在 system-configs GET 路由（server.ts:778）之前插入：

```typescript
app.get(
  '/api/admin/admin-users',
  asyncHandler(async (req, res) => {
    requireRole(req, ['super_admin']);
    const items = await prisma.adminUser.findMany({
      select: { id: true, username: true, displayName: true, role: true, status: true, createdAt: true, updatedAt: true },
      orderBy: { createdAt: 'asc' },
    });
    res.json({ items });
  }),
);
```

**Step 3: 实现 POST 新增接口**

```typescript
app.post(
  '/api/admin/admin-users',
  asyncHandler(async (req, res) => {
    const admin = requireRole(req, ['super_admin']);
    const input = validateBody(adminUserCreateSchema, req.body);
    const existing = await prisma.adminUser.findUnique({ where: { username: input.username } });
    if (existing) throw new HttpError(400, '用户名已存在');
    const created = await prisma.adminUser.create({
      data: {
        username: input.username,
        passwordHash: hashPassword(input.password),
        displayName: input.displayName,
        role: input.role,
      },
      select: { id: true, username: true, displayName: true, role: true, status: true },
    });
    await writeOperationLog({
      adminUserId: admin.id,
      action: 'admin_user.create',
      targetType: 'admin_users',
      targetId: created.id,
      afterValue: created,
      note: `新增管理员 ${created.username}`,
    });
    res.status(201).json(created);
  }),
);
```

**Step 4: 实现 PATCH 更新接口**

```typescript
app.patch(
  '/api/admin/admin-users/:id',
  asyncHandler(async (req, res) => {
    const admin = requireRole(req, ['super_admin']);
    const input = validateBody(adminUserUpdateSchema, req.body);
    const before = await prisma.adminUser.findUnique({ where: { id: req.params.id } });
    if (!before) throw new HttpError(404, '管理员不存在');
    const data: Prisma.AdminUserUncheckedUpdateInput = {};
    if (input.role) data.role = input.role;
    if (input.status) data.status = input.status;
    if (input.displayName) data.displayName = input.displayName;
    if (input.password) data.passwordHash = hashPassword(input.password);
    const updated = await prisma.adminUser.update({
      where: { id: req.params.id },
      data,
      select: { id: true, username: true, displayName: true, role: true, status: true },
    });
    await writeOperationLog({
      adminUserId: admin.id,
      action: 'admin_user.update',
      targetType: 'admin_users',
      targetId: updated.id,
      beforeValue: { ...before, passwordHash: '[redacted]' },
      afterValue: updated,
      note: `更新管理员 ${updated.username}`,
    });
    res.json(updated);
  }),
);
```

**注意**：beforeValue 中 `passwordHash` 必须脱敏，不能写入日志。上面已用 `[redacted]` 替换。

**Step 5: 验证**

```bash
# 启动 API（ALLOW_DEV_ADMIN=true pnpm dev:api）
curl -s http://localhost:4000/api/admin/admin-users -H "Authorization: Bearer <token>"
# 期望返回 seed-admin
```

**Step 6: Commit**

```bash
git add apps/api/src/server.ts
git commit -m "feat(api): 新增管理员管理接口（列表/新增/更新）"
```

---

### Task M1-3: 系统设置页 /settings（前端）

**Files:**
- Create: `apps/admin/src/pages/SettingsPage.tsx`
- Modify: `apps/admin/src/App.tsx`（注册路由 + 菜单项 + selectedKey）
- Modify: `apps/admin/src/api.ts`（无需改，已有 apiGet/apiSend）
- Modify: `apps/admin/src/types.ts`（新增 AdminUserListItem 类型，含 status/createdAt）

**Step 1: 新增类型**

types.ts 新增：

```typescript
export interface AdminUserListItem {
  id: string;
  username: string;
  displayName: string;
  role: string;
  status: 'active' | 'disabled';
  createdAt: string;
  updatedAt: string;
}

export interface SystemConfigItem {
  id: string;
  configKey: string;
  configValue: unknown;
  description?: string | null;
}
```

**Step 2: 实现 SettingsPage**

`SettingsPage` 用 Tabs 分三块：
1. **合规文案**：读 `GET /api/admin/system-configs`，找 `compliance_notice` 和 `official_action_text`，提供 Input.TextArea 编辑，`PUT /api/admin/system-configs/:key` 保存。仅 super_admin 可编辑（通过 admin role 判断，见 M1-6 Context）。
2. **管理员列表**：`GET /api/admin/admin-users`，Table 展示用户名/显示名/角色/状态，操作列「编辑」「启用/禁用」。新增按钮打开 Modal 表单（用户名/密码/显示名/角色 Select），调 POST。403 时隐藏整块（非 super_admin）。
3. **关于**：版本号、环境提示、链接到 `/logs`。

**Step 3: 注册路由与菜单**

App.tsx `Shell` 的 `items` 数组新增（`SettingOutlined` 需 import）：

```tsx
{
  key: '/settings',
  icon: <SettingOutlined />,
  label: <Link to="/settings">系统设置</Link>,
},
```

`selectedKey` 逻辑追加：`location.pathname.startsWith('/settings') ? '/settings' : ...`。

`Routes` 内新增：`<Route path="/settings" element={<SettingsPage />} />`。

**Step 4: 验证**

```bash
pnpm dev:admin
# 浏览器访问 /settings，测试编辑合规文案、新增管理员
```

**Step 5: Commit**

```bash
git add apps/admin/src/
git commit -m "feat(admin): 新增系统设置页（合规文案/管理员管理）"
```

---

### Task M1-4: 工具与内容配置页 /content（前端）

**Files:**
- Create: `apps/admin/src/pages/ContentPage.tsx`
- Modify: `apps/admin/src/App.tsx`（路由 + 菜单）
- Modify: `apps/admin/src/pages/EventEditPage.tsx`（城市/距离改 Select，从配置读取）

**Step 1: 定义配置 key 规范**

约定 system_config 的 key：
- `available_cities`：JSON 数组，如 `["广州","深圳","佛山",...]`
- `distance_options`：JSON 数组，如 `["5K","10K","半马","全马","欢乐跑"]`
- `decision_tag_options`：JSON 数组，如 `["新手友好","适合 PB","风景路线",...]`
- `checklist_templates`：JSON 对象，key 为距离类型（`general`/`5K`/`10K`/`半马`/`全马`），value 为 checklist item 数组

**Step 2: 实现 ContentPage**

Tabs 四块：
1. **城市配置**：可增删的城市 chip 列表（用 AntD `Select mode="tags"` 或 `Input` + 添加按钮），保存为 `available_cities`。
2. **距离项目**：同上，保存为 `distance_options`。
3. **决策标签**：同上，保存为 `decision_tag_options`。
4. **赛前清单模板**：按距离类型分 Tab，每组用简化版 ChecklistEditor（复用 EventEditPage 的 Form.List 模式），保存为 `checklist_templates`。

每块统一模式：`useEffect` 读 configs → 本地 state 编辑 →「保存」按钮 PUT 对应 key。提供「重置为默认」按钮恢复内置默认值。

**Step 3: 抽取配置读取 hook**

`apps/admin/src/hooks/useSystemConfig.ts`：

```typescript
export function useSystemConfig<T>(key: string, defaultValue: T) {
  // 读取 system_configs，解析 configValue，fallback 到 defaultValue
}
```

供 ContentPage 和 EventEditPage 复用。

**Step 4: EventEditPage 城市距离改下拉**

EventEditPage 中城市 `<Input>`（原 App.tsx:607-609）改为 `<Select>` options 来自 `available_cities`（支持自由输入：`Select showSearch mode="tags"`）。
距离 `<Input>`（原 App.tsx:614-618）改为 `<Select mode="multiple">` options 来自 `distance_options`。
提交时 `distanceItems` 不再 `splitComma`，直接用数组。

**Step 5: 注册路由与菜单**

`ToolOutlined` 图标，菜单 key `/content`。

**Step 6: 验证**

配置改城市/距离 → 赛事编辑页下拉同步更新。

**Step 7: Commit**

```bash
git add apps/admin/src/
git commit -m "feat(admin): 新增内容配置页，赛事编辑城市距离改下拉"
```

---

### Task M1-5: 独立操作日志页 /logs（前端）

**Files:**
- Create: `apps/admin/src/pages/LogsPage.tsx`
- Modify: `apps/admin/src/App.tsx`（路由 + 菜单）

**Step 1: 实现 LogsPage**

```tsx
// 结构：toolbar（筛选） + Table（分页）
// 筛选：action（Select：event.create/event.update/event.publish/...）、targetType（Select：events/feedback/config/admin_users）、关键词
// 表格列：时间、操作人、动作、目标类型、目标 ID、说明
// 分页：调用 /api/admin/operation-logs?page=&pageSize=&action=&targetType=
```

使用 `apiGet` 调 `/api/admin/operation-logs`，携带分页参数。Table 分页器 onChange 重新请求。

**Step 2: 注册路由与菜单**

`FileTextOutlined` 图标，菜单 key `/logs`。`selectedKey` 追加 `/logs` 判断。

**Step 3: 验证**

访问 /logs，可分页、筛选查询操作历史。

**Step 4: Commit**

```bash
git add apps/admin/src/
git commit -m "feat(admin): 新增独立操作日志页"
```

---

### Task M1-6: 归档操作前端入口

**Files:**
- Modify: `apps/admin/src/pages/EventsPage.tsx`（操作列加归档按钮）

**Step 1: 加归档按钮**

在 EventsPage 操作列（原 App.tsx:456-490）的 Space 内，`下架` 按钮后新增：

```tsx
<Button
  size="small"
  onClick={() => changeStatus(record, 'archive', '归档赛事')}
>
  归档
</Button>
```

**Step 2: 发布状态为 archived 时显示「恢复发布」**

操作列渲染逻辑：当 `record.publishStatus === 'archived'` 时，显示「恢复发布」按钮（走 publish 流程），隐藏「归档」。可简化为：archived 状态只显示「编辑」「恢复发布」「日志」。

**Step 3: 验证**

归档赛事 → 前端公开列表不可见（后端已过滤）→ 后台点「恢复发布」可恢复。

**Step 4: Commit**

```bash
git add apps/admin/src/pages/EventsPage.tsx
git commit -m "feat(admin): 赛事库新增归档操作与恢复发布"
```

---

### Task M1-7: 角色化 UI（按角色隐藏按钮）

**Files:**
- Create: `apps/admin/src/context/AdminContext.tsx`
- Modify: `apps/admin/src/App.tsx`（ProtectedShell 提供 Context）
- Modify: 各页面（用 useAdmin 控制按钮显示）

**Step 1: 创建 AdminContext**

```typescript
// apps/admin/src/context/AdminContext.tsx
import { createContext, useContext } from 'react';
import { AdminUser } from '../types';

const AdminContext = createContext<AdminUser | null>(null);

export function AdminProvider({ admin, children }: { admin: AdminUser | null; children: React.ReactNode }) {
  return <AdminContext.Provider value={admin}>{children}</AdminContext.Provider>;
}

export function useAdmin() {
  return useContext(AdminContext);
}

// 权限判断辅助：按 V0.1 需求说明 §11 权限矩阵
export function can(action: string, role: string | undefined): boolean {
  if (!role) return false;
  if (role === 'super_admin') return true;
  const map: Record<string, string[]> = {
    create_event: ['event_operator'],
    edit_event: ['event_operator', 'content_reviewer'],
    publish_event: ['event_operator'],
    hide_event: ['event_operator'],
    handle_feedback: ['event_operator', 'content_reviewer'],
    manage_settings: [],
    view: ['event_operator', 'content_reviewer', 'readonly'],
  };
  return (map[action] || []).includes(role);
}
```

**Step 2: ProtectedShell 包裹 Provider**

App.tsx `ProtectedShell` 中 `<Shell>` 外包 `<AdminProvider admin={admin}>`。

**Step 3: 各页面按权限隐藏**

- EventsPage：「新增赛事」按钮 `can('create_event', role)`；操作列的发布/隐藏/下架/归档按钮 `can('publish_event', role)`；编辑按钮 `can('edit_event', role)`。
- SettingsPage：合规文案编辑、管理员管理整块 `can('manage_settings', role)`，否则只读。
- ContentPage：保存按钮 `can('manage_settings', role)`。
- QualityPage：处理按钮 `can('handle_feedback', role)`。

**Step 4: 验证**

用 readonly 账号登录 → 无写按钮；content_reviewer → 无新增/发布/设置；event_operator → 无设置写。

**Step 5: Commit**

```bash
git add apps/admin/src/
git commit -m "feat(admin): 角色化 UI，按权限矩阵隐藏操作按钮"
```

---

### Task M1-8: 枚举去重（代码卫生）

**Files:**
- Modify: `packages/shared/src/index.ts`（导出 `as const` 数组）
- Modify: `apps/api/src/server.ts`（引用 shared 数组替代本地 `as const`）

**Step 1: shared 导出数组**

packages/shared/src/index.ts 末尾新增：

```typescript
export const publishStatusValues = ['draft', 'published', 'hidden', 'offline', 'archived'] as const;
export const infoStatusValues = ['ai_generated', 'pending_verify', 'verified', 'user_flagged', 'source_error'] as const;
export const runJudgementValues = ['priority', 'watch', 'unverified'] as const;
export const signupStatusValues = ['signup_open', 'closing_soon', 'closed', 'not_started', 'unknown'] as const;
export const sourceLevelValues = ['official', 'trusted', 'secondary', 'unknown'] as const;
export const feedbackStatusValues = ['pending', 'handling', 'resolved', 'rejected'] as const;
export const adminRoleValues = ['super_admin', 'event_operator', 'content_reviewer', 'readonly'] as const;
```

**Step 2: server.ts 引用**

删除 server.ts:66-83 的本地 `as const` 定义，改为：

```typescript
import {
  publishStatusValues, infoStatusValues, runJudgementValues,
  signupStatusValues, sourceLevelValues, feedbackStatusValues,
} from '@worth-running/shared';
```

注意：Zod 的 `z.enum()` 需要类型为 `[string, ...string[]]`。由于 `as const` 数组是 readonly tuple，需用 `z.enum(publishStatusValues)` —— Zod 4 支持 readonly array，但当前用 Zod 3.23。若类型不兼容，用 `z.enum([...publishStatusValues])` 展开或断言。

**Step 3: 验证**

```bash
pnpm typecheck
# 若 Zod 类型报错，调整为 z.enum([...publishStatusValues] as [string, ...string[]])
```

**Step 4: Commit**

```bash
git add packages/shared/src/index.ts apps/api/src/server.ts
git commit -m "refactor: 枚举去重，server.ts 引用 shared 导出"
```

---

## 里程碑 M2：V0.1 小程序微调（赛前清单接接口）

### Task M2-1: 后端清单模板接口支持按类型 + 读配置

**Files:**
- Modify: `apps/api/src/server.ts`（扩展 `/api/checklist/templates`）

**Step 1: 接口支持 type 参数与配置读取**

替换 server.ts:954-971 的 `/api/checklist/templates` 实现：

```typescript
const defaultChecklistTemplates: Record<string, Array<{ groupName: string; itemName: string; itemStatus: string; sortOrder: number }>> = {
  general: [/* 现有 7 项 */],
  '5K': [/* 4 项 */],
  '10K': [/* 4 项 */],
  half: [/* 4 项 */],
  full: [/* 4 项 */],
};

app.get('/api/checklist/templates', asyncHandler(async (req, res) => {
  const type = String(req.query.type || 'general');
  // 优先读 system_config checklist_templates，fallback 到内置默认
  const config = await prisma.systemConfig.findUnique({ where: { configKey: 'checklist_templates' } });
  const templates = (config?.configValue as Record<string, unknown>) || defaultChecklistTemplates;
  const items = (templates[type] as Array<{ groupName: string; itemName: string; itemStatus: string; sortOrder: number }>) || templates.general || defaultChecklistTemplates.general;
  res.json({ items });
}));
```

**Step 2: 验证**

```bash
curl -s "http://localhost:4000/api/checklist/templates?type=half"
```

**Step 3: Commit**

```bash
git add apps/api/src/server.ts
git commit -m "feat(api): 清单模板接口支持按类型查询并读取配置"
```

---

### Task M2-2: 小程序清单页接接口（带兜底）

**Files:**
- Modify: `apps/miniapp/pages/checklist/index.ts`
- Modify: `apps/miniapp/utils/api.ts`（`getChecklistTemplates` 支持 type 参数）

**Step 1: api.ts 扩展**

```typescript
export function getChecklistTemplates(type?: string) {
  const query = type ? `?type=${encodeURIComponent(type)}` : '';
  return request<{ items: ChecklistItem[] }>(`/api/checklist/templates${query}`, { silent: true });
}
```

**Step 2: 清单页改为接口优先 + 硬编码兜底**

`pages/checklist/index.ts` 保留现有 `checklistByGroup` 作为 fallback。`onLoad` 和 `onGroupChange` 时调用接口，失败用本地数据：

```typescript
const typeKeyMap: Record<string, string> = { '通用清单': 'general', '5K': '5K', '10K': '10K', '半马': 'half', '全马': 'full' };

async loadItems() {
  const type = typeKeyMap[groups[this.data.groupIndex]] || 'general';
  this.setData({ loading: true });
  try {
    const result = await getChecklistTemplates(type);
    this.setData({ items: result.items, loading: false, error: '' });
  } catch {
    // 兜底：用本地硬编码
    this.setData({ items: checklistByGroup[groups[this.data.groupIndex]] || [], loading: false });
  }
},
onLoad() { this.loadItems(); },
onGroupChange(event) {
  const groupIndex = Number(event.detail.value);
  this.setData({ groupIndex });
  this.loadItems();
},
```

**Step 3: 验证**

后台改 `checklist_templates` 配置 → 小程序清单页同步；关掉 API → 仍有本地兜底。

**Step 4: Commit**

```bash
git add apps/miniapp/pages/checklist/index.ts apps/miniapp/utils/api.ts
git commit -m "feat(miniapp): 赛前清单页接入后端模板，保留本地兜底"
```

---

## 里程碑 M3：V0.2 分享传播版

### Task M3-1: 数据模型新增 share_records 表

**Files:**
- Modify: `packages/database/prisma/schema.prisma`
- Create: 迁移文件（`pnpm db:migrate` 自动生成）

**Step 1: schema 新增 model**

在 Event model（schema.prisma:66-105）的 relations 区追加：

```prisma
  shareRecords   ShareRecord[]
```

文件末尾新增 model：

```prisma
model ShareRecord {
  id        String   @id @default(cuid())
  userKey   String?  @map("user_key")
  eventId   String?  @map("event_id")
  shareType String   @map("share_type")
  scene     String
  createdAt DateTime @default(now()) @map("created_at")

  event Event? @relation(fields: [eventId], references: [id], onDelete: SetNull)

  @@index([eventId])
  @@index([createdAt])
  @@map("share_records")
}
```

**Step 2: 生成迁移**

```bash
pnpm db:migrate -- --name share_records
# 或 pnpm --filter @worth-running/database exec prisma migrate dev --name share_records
```

**Step 3: 重新生成 client**

```bash
pnpm db:generate
```

**Step 4: 验证**

```bash
# 确认迁移文件生成在 packages/database/prisma/migrations/
ls packages/database/prisma/migrations/
```

**Step 5: Commit**

```bash
git add packages/database/
git commit -m "feat(db): 新增 share_records 表记录分享行为"
```

---

### Task M3-2: 后端分享记录接口

**Files:**
- Modify: `apps/api/src/server.ts`

**Step 1: 定义 schema**

schema 区新增：

```typescript
const shareRecordSchema = z.object({
  userKey: z.string().trim().min(1).max(100),
  eventId: z.string().trim().min(1).optional(),
  shareType: z.enum(['page_share', 'image_generate']),
  scene: z.enum(['event_detail', 'after_favorite', 'home', 'events']),
});
```

**Step 2: POST 公开接口**

在 `/api/feedback` 路由（server.ts:934）后插入：

```typescript
app.post(
  '/api/share-records',
  asyncHandler(async (req, res) => {
    const input = validateBody(shareRecordSchema, req.body);
    const record = await prisma.shareRecord.create({
      data: {
        userKey: input.userKey,
        eventId: input.eventId || null,
        shareType: input.shareType,
        scene: input.scene,
      },
    });
    res.status(201).json({ id: record.id });
  }),
);
```

**Step 3: 后台统计接口**

在 admin 路由区（operation-logs 之后）新增：

```typescript
app.get(
  '/api/admin/share-records/stats',
  asyncHandler(async (req, res) => {
    requireRole(req, ['super_admin', 'event_operator', 'content_reviewer', 'readonly']);
    const [total, pageShares, imageGenerates, topEvents] = await Promise.all([
      prisma.shareRecord.count(),
      prisma.shareRecord.count({ where: { shareType: 'page_share' } }),
      prisma.shareRecord.count({ where: { shareType: 'image_generate' } }),
      prisma.shareRecord.groupBy({
        by: ['eventId'],
        _count: { _all: true },
        orderBy: { _count: { id: 'desc' } },
        take: 10,
      }),
    ]);
    // 补充 topEvents 的赛事名称
    const eventIds = topEvents.map((item) => item.eventId).filter(Boolean) as string[];
    const events = await prisma.event.findMany({
      where: { id: { in: eventIds } },
      select: { id: true, eventName: true, city: true },
    });
    const eventMap = new Map(events.map((event) => [event.id, event]));
    const topEventsWithName = topEvents.map((item) => ({
      event: eventMap.get(item.eventId || ''),
      count: item._count._all,
    }));
    res.json({ total, pageShares, imageGenerates, topEvents: topEventsWithName });
  }),
);
```

**Step 4: 验证**

```bash
curl -s -X POST http://localhost:4000/api/share-records -H "Content-Type: application/json" \
  -d '{"userKey":"test","eventId":"<id>","shareType":"page_share","scene":"event_detail"}'
# 期望 201
```

**Step 5: Commit**

```bash
git add apps/api/src/server.ts
git commit -m "feat(api): 新增分享记录上报与统计接口"
```

---

### Task M3-3: 微信页面分享（小程序 onShareAppMessage）

**Files:**
- Modify: `apps/miniapp/pages/event-detail/index.ts`
- Modify: `apps/miniapp/pages/home/index.ts`（可选基础分享）
- Modify: `apps/miniapp/pages/events/index.ts`（可选基础分享）
- Modify: `apps/miniapp/utils/api.ts`（新增 `recordShare`）

**Step 1: api.ts 新增 recordShare**

```typescript
export function recordShare(data: {
  userKey: string;
  eventId?: string;
  shareType: 'page_share' | 'image_generate';
  scene: string;
}) {
  return request('/api/share-records', { method: 'POST', data, silent: true });
}
```

**Step 2: event-detail 新增分享**

`pages/event-detail/index.ts` Page 对象内新增：

```typescript
onShareAppMessage() {
  const event = this.data.event;
  if (event) {
    // 静默记录分享行为
    recordShare({
      userKey: this.data.userKey,
      eventId: event.id,
      shareType: 'page_share',
      scene: 'event_detail',
    }).catch(() => {});
  }
  return {
    title: event ? `这场值得跑吗？${event.eventName}` : '哪场值得跑｜大湾区跑步赛事决策工具',
    path: event ? `/pages/event-detail/index?id=${event.id}` : '/pages/home/index',
  };
},
onShareTimeline() {
  const event = this.data.event;
  return {
    title: event ? `这场值得跑吗？${event.eventName}` : '哪场值得跑',
    query: event ? `id=${event.id}` : '',
  };
},
```

**Step 3: home / events 基础分享（可选）**

两页加 `onShareAppMessage` 返回固定标题和首页 path。

**Step 4: 验证**

开发者工具 → 详情页右上角「...」→ 转发给朋友，可生成卡片，点开直达赛事。

**Step 5: Commit**

```bash
git add apps/miniapp/
git commit -m "feat(miniapp): 赛事详情页支持微信转发与朋友圈分享"
```

---

### Task M3-4: 赛事决策卡分享图页面（Canvas 2D）

**Files:**
- Create: `apps/miniapp/pages/share-card/index.ts`
- Create: `apps/miniapp/pages/share-card/index.wxml`
- Create: `apps/miniapp/pages/share-card/index.wxss`
- Create: `apps/miniapp/pages/share-card/index.json`
- Modify: `apps/miniapp/app.json`（注册页面）
- Modify: `apps/miniapp/pages/event-detail/index.wxml`（底部栏加「分享图」按钮）
- Modify: `apps/miniapp/pages/event-detail/index.ts`（加 `openShareCard` 跳转）

**Step 1: app.json 注册页面**

pages 数组追加 `"pages/share-card/index"`。

**Step 2: event-detail 底部栏加按钮**

`pages/event-detail/index.wxml` 的 `bottom-bar`（L54-58）在「反馈纠错」前插入：

```xml
<button class="secondary-button" bindtap="openShareCard">分享图</button>
```

`pages/event-detail/index.ts` 新增方法：

```typescript
openShareCard() {
  if (!this.data.event) return;
  wx.navigateTo({ url: `/pages/share-card/index?id=${this.data.id}` });
},
```

**Step 3: share-card 页面骨架**

`index.json`：
```json
{
  "navigationBarTitleText": "分享卡片",
  "usingComponents": {
    "loading-state": "../../components/loading-state/index",
    "empty-state": "../../components/empty-state/index"
  }
}
```

`index.wxml`：
```xml
<view class="page share-card-page">
  <loading-state wx:if="{{loading}}" text="生成中" />
  <block wx:elif="{{error}}">
    <empty-state title="生成失败" description="{{error}}" />
  </block>
  <block wx:else>
    <canvas type="2d" id="shareCanvas" class="share-canvas" style="width:{{canvasW}}px;height:{{canvasH}}px;"></canvas>
    <view class="actions">
      <button class="primary-button" bindtap="saveImage" loading="{{saving}}">保存到相册</button>
      <button class="secondary-button" open-type="share">分享给好友</button>
    </view>
    <view class="notice">AI 整理，仅供参考｜报名以官方为准</view>
  </block>
</view>
```

**Step 4: Canvas 绘制逻辑（index.ts）**

核心实现要点：
- `onLoad(query)` 取 id，调 `getEventDetail(id)` 获取数据
- 初始化 Canvas 2D：`wx.createSelectorQuery().select('#shareCanvas').fields({ node: true, size: true })`
- 按 dpr 缩放：`canvas.width = width * dpr; ctx.scale(dpr, dpr)`
- 绘制顺序：背景 → 标题区（产品名「哪场值得跑」）→ 赛事名（大字）→ 城市/日期/距离 → 报名状态+倒计时 → 3 个标签 → 跑者摘要（自动换行）→ 小程序码（调 M3-5 的接口或用静态图）→ 底部合规提示
- 文字换行辅助函数：按字符宽度逐字累加，超宽换行
- 绘制完成后 `wx.canvasToTempFilePath` 得到临时图片路径，存 `this.tempFilePath`

**Step 5: 保存到相册**

```typescript
async saveImage() {
  if (!this.data.tempFilePath) return;
  this.setData({ saving: true });
  try {
    // 先检查授权
    const setting = await wx.getSetting();
    if (!setting.authSetting['scope.writePhotosAlbum']) {
      await wx.authorize({ scope: 'scope.writePhotosAlbum' });
    }
    await wx.saveImageToPhotosAlbum({ filePath: this.data.tempFilePath });
    wx.showToast({ title: '已保存到相册', icon: 'success' });
    // 记录分享行为
    recordShare({ userKey: this.data.userKey, eventId: this.data.id, shareType: 'image_generate', scene: 'event_detail' }).catch(() => {});
  } catch (error) {
    if ((error as any).errMsg?.includes('auth deny')) {
      wx.showModal({
        title: '需要相册权限',
        content: '保存图片需要相册权限，是否前往设置开启？',
        confirmText: '去设置',
        success: (res) => { if (res.confirm) wx.openSetting(); },
      });
    } else {
      wx.showToast({ title: '保存失败', icon: 'none' });
    }
  } finally {
    this.setData({ saving: false });
  }
}
```

**Step 6: 样式（index.wxss）**

画布固定 375×667（逻辑像素），背景白色，圆角。

**Step 7: 验证**

详情页 → 点「分享图」→ 进入分享卡页 → Canvas 绘制 → 保存到相册。

**Step 8: Commit**

```bash
git add apps/miniapp/
git commit -m "feat(miniapp): 新增赛事决策卡分享图（Canvas 2D 生成与保存）"
```

---

### Task M3-5: 小程序码生成（后端 wxacode 模块）

**Files:**
- Create: `apps/api/src/wxacode.ts`
- Modify: `apps/api/src/server.ts`（引入 wxacode 路由）
- Modify: `apps/miniapp/pages/share-card/index.ts`（绘制时加载小程序码）
- Modify: `.env.example`（新增 WX_APPID / WX_APPSECRET）

**Step 1: 创建 wxacode 模块**

`apps/api/src/wxacode.ts`：
- `getAccessToken()`：调 `https://api.weixin.qq.com/cgi-bin/token`，内存缓存（TTL 7000s），需 `WX_APPID` + `WX_APPSECRET`
- `getMiniProgramCode(scene, page)`：调 `https://api.weixin.qq.com/wxa/getwxacodeunlimit`，返回 PNG Buffer
- 降级：无配置时返回 null（前端用静态图）

```typescript
// apps/api/src/wxacode.ts
let cachedToken: { value: string; expiresAt: number } | null = null;

export async function getAccessToken(): Promise<string | null> {
  const appid = process.env.WX_APPID;
  const secret = process.env.WX_APPSECRET;
  if (!appid || !secret) return null;
  if (cachedToken && cachedToken.expiresAt > Date.now() + 60000) return cachedToken.value;
  const url = `https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential&appid=${appid}&secret=${secret}`;
  const res = await fetch(url);
  const data = await res.json() as { access_token?: string; expires_in?: number; errcode?: number };
  if (!data.access_token) return null;
  cachedToken = { value: data.access_token, expiresAt: Date.now() + (data.expires_in || 7200) * 1000 };
  return cachedToken.value;
}

export async function getMiniProgramCode(scene: string, page: string): Promise<Buffer | null> {
  const token = await getAccessToken();
  if (!token) return null;
  const res = await fetch(`https://api.weixin.qq.com/wxa/getwxacodeunlimit?access_token=${token}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ scene, page, width: 280, check_path: false }),
  });
  const contentType = res.headers.get('content-type') || '';
  if (!contentType.includes('image')) return null;
  const arrayBuffer = await res.arrayBuffer();
  return Buffer.from(arrayBuffer);
}
```

**Step 2: server.ts 加路由**

```typescript
import { getMiniProgramCode } from './wxacode.js'; // ESM 注意 .js

app.get(
  '/api/wxacode',
  asyncHandler(async (req, res) => {
    const eventId = String(req.query.eventId || '');
    if (!eventId) throw new HttpError(400, 'eventId 不能为空');
    const buffer = await getMiniProgramCode(`id=${eventId}`, 'pages/event-detail/index');
    if (!buffer) throw new HttpError(503, '小程序码服务暂不可用');
    res.setHeader('Content-Type', 'image/png');
    res.setHeader('Cache-Control', 'public, max-age=86400');
    res.send(buffer);
  }),
);
```

**Step 3: share-card 页加载小程序码**

绘制时，若后端可用，`wx.getImageInfo` 加载 `${apiBaseUrl}/api/wxacode?eventId=xxx` 绘制；失败用本地静态图 `assets/share-qr.png`（需准备一张通用小程序码占位图）。

**Step 4: .env.example 更新**

```
WX_APPID=""
WX_APPSECRET=""
```

**Step 5: 验证**

配置 WX_APPID/WX_APPSECRET 后，`curl http://localhost:4000/api/wxacode?eventId=xxx -o code.png` 得到 PNG。

**Step 6: Commit**

```bash
git add apps/api/src/ apps/miniapp/ .env.example
git commit -m "feat: 小程序码生成（后端调微信API + 分享图集成）"
```

---

### Task M3-6: 后台分享数据统计页

**Files:**
- Create: `apps/admin/src/pages/ShareStatsPage.tsx`
- Modify: `apps/admin/src/App.tsx`（路由 + 菜单）

**Step 1: 实现 ShareStatsPage**

```tsx
// 结构：
// - 顶部统计卡片：累计分享总数、页面分享次数、图片生成次数
// - 赛事分享 Top 榜表格：赛事名称、城市、分享次数
// 数据来源：GET /api/admin/share-records/stats
```

**Step 2: 注册路由与菜单**

`ShareAltOutlined` 或 `BarChartOutlined` 图标，菜单 key `/share-stats`。

**Step 3: 验证**

触发几次分享 → 后台 /share-stats 可见数据。

**Step 4: Commit**

```bash
git add apps/admin/src/
git commit -m "feat(admin): 新增分享数据统计页"
```

---

### Task M3-7: 更新文档与检查清单

**Files:**
- Modify: `README.md`（V0.2 功能说明、新环境变量）
- Modify: `docs/PRODUCTION_RELEASE_CHECKLIST.md`（新增 V0.2 检查项）

**Step 1: README 更新**

- 「当前已完成」追加 V0.2 分享功能
- 「明确未做」移除分享卡片相关项
- 「小程序当前功能」追加：赛事详情分享、决策卡分享图、保存到相册
- 「API 运行环境与安全配置」追加 WX_APPID / WX_APPSECRET 说明

**Step 2: 检查清单更新**

新增 V0.2 发版检查：
- [ ] 分享图生成正常，含小程序码
- [ ] 分享图底部保留合规提示
- [ ] 分享行为可被后台统计
- [ ] 相册授权拒绝时有引导

**Step 3: Commit**

```bash
git add README.md docs/
git commit -m "docs: 更新 V0.2 分享功能说明与发版检查清单"
```

---

## 最终验证（全量）

### M1 完成后
```bash
pnpm typecheck && pnpm build
# 后台 /settings /content /logs 可用，归档/角色化 UI 正常
```

### M2 完成后
```bash
# 后台改 checklist_templates → 小程序清单页同步
```

### M3 完成后（对应产品方案 §22.2 V0.2 触发标准）
- 详情页可转发给朋友/朋友圈，直达同一场赛事
- 可生成赛事决策卡分享图并保存到相册
- 分享图含小程序码、合规提示、跑者摘要
- 分享图生成成功率 ≥ 90%
- 分享行为被记录，后台可查看统计数据
- 分享图底部保留「AI 整理，仅供参考｜报名以官方为准」
- iOS/Android 多机型排版正常

---

## 注意事项

1. **小程序码需 AppSecret**：M3-5 若 AppSecret 不可用，先用静态码降级，不阻塞其他任务。
2. **Canvas 机型兼容**：M3-4 需在 iOS/Android 多机型测试文字截断、图片加载、dpr 缩放。
3. **保存相册授权**：M3-4 需处理 `scope.writePhotosAlbum` 拒绝场景，引导 openSetting。
4. **不诱导分享**：严格遵守产品方案 §13.2，不做分享解锁/奖励/助力。
5. **后端单文件**：server.ts 已近 1000 行，M3 新增内容较多，wxacode 已拆独立文件；若 server.ts 超 1200 行考虑进一步拆分路由。
6. **ESM 导入路径**：后端 `"type": "module"`，`import` 本地文件需带 `.js` 扩展名（如 `./wxacode.js`）。
7. **Zod enum 与 readonly array**：M1-8 用 shared `as const` 数组时，若 `z.enum()` 类型不兼容，用 `z.enum([...values] as [string, ...string[]])`。
8. **操作日志脱敏**：M1-2 管理员接口的 beforeValue 必须脱敏 passwordHash。
9. **小步提交**：每个 Task 完成后即 commit，保持可回滚。
